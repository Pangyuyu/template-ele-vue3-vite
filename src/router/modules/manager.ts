import Main from "@/layouts/main.vue";
const manager = {
  path: "/manager",
  name: "areaZone",
  component: Main,
  redirect: "/manager/areaZone",
  children: [
    
  ],
};
export default manager;
